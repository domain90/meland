<!DOCTYPE html>
<html>
    <head>
        <Title> Edita tu comentario - Meland </Title>

<% include ../partials/header %>

<div class="container">
    <div class="row">
        <h1 style="text-align: center;">Edita tu comentario</h1>
        <div style="width: 30%; margin: 30px auto;">
             <form method="POST" action="/gags/<%= gag_id %>/<%= comment.id %>?_method=PUT" style="width: 80%; margin: 30px auto;">
                <div class="form-group">
                    <label for="">Texto</label>
                    <input class="form-control" type="text" name="comment[text]" value=<%= comment.text %>>
                </div>
                <div class="form-group">
                     <button class="btn btn-lg btn-primary btn-block">Cambiar</button>
                </div>
            </form>
            <a href="/">Cancelar</a>
        </div>
      
    </div>
</div>

<% include ../partials/footer %>