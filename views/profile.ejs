<!DOCTYPE html>
<html>
    <head>
        <Title> Mi perfil - Meland </Title>

<% include ./partials/header %>

<div class="container profile-container">
	<div class="row">
		<div class="col-6">
			<form method="POST" action="/profile/<%= currentUser.id %>?_method=PUT" enctype="multipart/form-data">
				<h2>Mi perfil</h2>
				<div class="form-group">
					<label for="Avatar">Avatar</label>
					<label for="file-upload" class="custom-file-upload" name="profile">
					    <img src="<%= currentUser.avatar %>" alt="" id="currentAvatar">
					</label>
					<input type="file" id="file-upload" name="profile" onchange="document.getElementById('currentAvatar').src = window.URL.createObjectURL(this.files[0])"/>
				</div>
				<div class="form-group">
					<label for="">Usuario</label>
				    <input type="text" name="username" class="form-control" value="<%= currentUser.username %>">
				</div>
				<div class="form-group">
					<label for="">Contraseña</label>
				    <input type="password" name="password" class="form-control" >
				</div>
				<div class="form-group">
					<label for="">Email</label>
				    <input type="text" name="email" class="form-control" value="<%= currentUser.email %>">
				</div>
				<button class="btn btn-primary">Actualizar</button>
			</form>

			<h2>Mi contenido</h2>
			<% Gags.forEach(function(gag){ %>
			    <div class="card">
				    <div class="head-title">
				        <h3 class="gag-title"><%= gag["title"] %></h3>
				    </div>
				    <div class="content-img">
				        <a href="/gags/<%= gag['_id'] %>">
				          <img id="meme-content" src="<%= gag['image'] %>">
				        </a>
				    </div>
				    <div class="meta">
				        <div class="meta-info">
				          <p class="left-floated"><%= gag["views"] %> Vistas</p>
				          -
				          <p class="left-floated"><%= gag["commentsNumber"] %> Comentarios</p>
				     	</div>
				    </div>
				    <div class="cta">
				        <div class="left-floated">
				          <a href="/gags/<%= gag['_id'] %>" class="comment btn btn-default">
				            <i class="fa fa-commenting" aria-hidden="true"></i>
				          </a>
				        </div>
				    </div>
				    <hr>  
			    </div>
		    <% }) %>
		</div>
		
	</div>
	
	
</div>



<% include ./partials/footer %>




















